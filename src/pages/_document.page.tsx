import { Html, Head, Main, NextScript } from "next/document";

export default () => {
  return (
    <Html lang="en">
      <Head />
      <body>
        {/* pages? */}
        {/* pages? */}
        {/* pages? */}
        <Main />
        <NextScript />
      </body>
    </Html>
  );
};
